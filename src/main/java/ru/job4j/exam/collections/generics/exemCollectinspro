1. Что такое generics?
это параметризация типов - т.е. тип данных указан в виде папаметра.
когда в одном классе типы других класлв интерфейсоа методов  можно определят в виде пааметров и
 повторно их сипользовать.
 для обобщения коллекций дженерики самый безопасный способ, для предотвращения
 ClassCastException в связи с неправильным приведением типов.

   2. Что такое wild cards?
   Тип дженерика применяется для решения проблем совместимости коллекций<?>
    используются в сигнатуре методов, но для параметризации класса - нет!

    3. Что такое bounded wild cards?
     ограничение сверху и ограничиваться классом Predator.
     ограничение снизу и ограничиваться классом Predator.
    Тоже самое что и wild cards но, с extends, - если нужно использоваь в коллекции
    данные дрцгих родтвенных коллекций мы будем прописывать в типе испольуземой коллекции
     Iterator<? extends Person>

        4. Что такое unbounded wild cards?
 без ограничений, т.е. в него можно передавать коллекцию, которая хранит любые типы.


         5. Где хранится информация про Generics?
         Только в исходном коде до момента компиляции.


          6. Как можно получить тип Generics?
          .getGenericSuperclass()
          Class<T> t = (Class<T>) ((ParameterizedType) getClass().getGenericSuperclass()).getActualTypeArguments()[0];

  7. Что такое итератор?

